export type dbData = dbItem[];

export interface dbItem {
  Color: any;
  Grade: string;
  ID: number;
  Word: string;
  Live: string;
  Sentence: string;
  Level: string;
}
export type random = {
  random: boolean;
};
